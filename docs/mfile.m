function y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in yfunction y = special_function(x, y, options)

  x
  y
  
  y = x+10
  return y
  %adds 10 to x and stores it in y

